import org.springframework.core.env.MapPropertySource;
import org.springframework.core.env.PropertySource;
import org.springframework.core.env.PropertySources;
import org.springframework.core.io.support.ResourcePropertySource;

import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

/**
 * Created by zhi.zhang on 2017/6/28.
 */
public class Test {

    @org.junit.Test
    public void test() throws IOException {
        Map<String, Object> map = new HashMap<>();
        map.put("test","test");
        PropertySource propertySources = new MapPropertySource("map",map);
        System.out.println(propertySources.getProperty("test"));
        ResourcePropertySource propertySource2 = new ResourcePropertySource("resource", "classpath:log4j.properties"); //name, location
        System.out.println(propertySource2.getProperty("log4j.rootCategory"));
    }
}
